import boss


def test_add_two_numbers(): #this needs to be a different name with test at the start
    assert boss.add_two_numbers(1,3) == 4 #put values in, then what you expect the outcome to be



